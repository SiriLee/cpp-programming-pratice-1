#include <bits/stdc++.h>

using namespace std;
using ll = long long;

ll getCount(int n) {
    ll rounds = (1 << n) - 1;
    ll num = 0;
    while (rounds--) {
        cout <<  << endl;
    }
}

int main() {
    int n;
    while (cin >> n) {
        if (n == -1) break;
        ll count = 1 << n;
    }
    return 0;
}